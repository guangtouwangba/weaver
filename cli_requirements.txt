# RAG CLI Client - 客户端专用依赖包
# 核心CLI框架
click>=8.0.0          # 强大的CLI框架，支持命令组和参数验证
rich>=13.0.0          # 漂亮的终端输出，支持表格、面板、进度条等
tqdm>=4.65.0          # 高级进度条，用于文件批量处理

# 增强功能依赖
python-dateutil>=2.8.0  # 日期时间处理
tabulate>=0.9.0         # 表格格式化（备用方案）
colorama>=0.4.0         # 跨平台彩色输出支持