{% if language == "en" %}
Optimize this Markdown outline for clarity and organization.

Original:
```markdown
{{ content }}
```

## ⚠️ CRITICAL: Source Marker Preservation

**You MUST preserve ALL source markers `[TIME:MM:SS]` and `[PAGE:X]` exactly as they appear!**

These markers link content to original sources. Removing them breaks navigation functionality.

Example - CORRECT:
- Input: `- AI Agent plans tasks autonomously [TIME:05:30]`
- Output: `- AI Agent plans tasks autonomously [TIME:05:30]`

Example - WRONG (DO NOT DO THIS):
- Input: `- AI Agent plans tasks autonomously [TIME:05:30]`
- Output: `- AI Agent plans tasks autonomously` ← WRONG! Marker removed!

## Optimization Rules

1. **Deduplication**: Remove parent-child with same name, remove duplicate siblings, merge similar content
2. **Structure**: Keep to 3-4 levels max, ensure logical parent-child relationships
3. **Cleanup**: Remove greetings/acknowledgements, abstract categories; node names ≤ 35 chars
4. **Preserve**: Names, places, numbers, dates, causality
5. **MANDATORY**: Keep ALL `[PAGE:X]` and `[TIME:MM:SS]` markers at the end of each line

## Format
- # for root heading
- - with 2-space indents

**IMPORTANT: Output ONLY the optimized Markdown. Start directly with # heading. No explanations.**

{% else %}
优化以下 Markdown 大纲，使其更清晰、更有条理。

原始大纲:
```markdown
{{ content }}
```

## ⚠️ 关键要求：必须保留来源标记

**你必须保留所有 `[TIME:MM:SS]` 和 `[PAGE:X]` 来源标记，不得删除！**

这些标记用于链接到原始内容。删除它们会导致导航功能失效。

示例 - 正确做法:
- 输入: `- AI Agent 自主规划任务 [TIME:05:30]`
- 输出: `- AI Agent 自主规划任务 [TIME:05:30]`

示例 - 错误做法（禁止这样做）:
- 输入: `- AI Agent 自主规划任务 [TIME:05:30]`
- 输出: `- AI Agent 自主规划任务` ← 错误！标记被删除了！

## 优化规则

1. **去重**: 删除父子同名节点、删除同级重复、合并相似内容
2. **结构**: 控制在 3-4 层深度，确保父子逻辑关系
3. **清理**: 删除致谢寒暄、抽象分类词；节点名 ≤ 30 字符
4. **保留**: 人名地名公司名、数字日期、因果关系
5. **强制要求**: 保留每行末尾的所有 `[PAGE:X]` 和 `[TIME:MM:SS]` 标记

## 格式
- # 根标题
- - 和 2空格缩进

**重要：只输出优化后的 Markdown。直接以 # 标题开始。不要解释。**

{% endif %}
