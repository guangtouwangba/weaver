1. Add `isInspirationDockVisible` and `setInspirationDockVisible` to `StudioContext`.
2. Update `InspirationDock` to include a close button.
3. Update `CanvasToolbar` to include a toggle button.
4. Verify interactions.
