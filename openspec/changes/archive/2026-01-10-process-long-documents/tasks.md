# Tasks

- [ ] Backend: Implement `RecursiveMindmapGenerator` service implementing ToM strategy @backend
- [ ] Backend: Add `MindmapMerger` utility using vector embeddings for semantic deduplication @backend
- [ ] Backend: Update `MindmapAgent` to route long documents to the recursive generator @backend
- [ ] Backend: Integrate `VectorStore` for temporary node embedding storage during generation @backend
- [ ] Frontend: Add progress indicator for multi-stage generation (Map -> Reduce -> Finalize) @frontend
- [ ] Test: Verify processing of >50 page PDF (synthetic test) @qa
