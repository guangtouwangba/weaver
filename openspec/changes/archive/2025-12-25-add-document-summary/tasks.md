## 1. Backend Implementation (Agents)
- [x] 1.0 Check Current summary agent implementation and refactor if necessary.
- [x] 1.1 Create `SummaryAgent` in backend to accept document IDs and return structured summary (strategy, insights, narrative).
- [x] 1.2 Implement API endpoint to trigger summary generation.

## 2. Frontend Implementation (Studio)
- [x] 2.1 Update `ResourceSidebar` to support multi-select and active state styling for documents (matching `document_active.jpg`).
- [x] 2.2 Update `InspirationDock` to include the "Summarize" action button.
- [x] 2.3 Implement `SummaryCard` component matching the design (matching `summary_card.jpg`).
- [x] 2.4 Implement full-content view for the summary (matching `summary_full_content.jpg`).
- [x] 2.5 Integrate frontend with backend summary API.

