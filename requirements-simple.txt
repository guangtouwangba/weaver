# Simplified Requirements for ArXiv Paper Fetcher
# Core dependencies only

# ArXiv API client
arxiv==2.1.3

# YAML configuration
PyYAML==6.0.2

# HTTP requests for PDF downloads
requests==2.32.3

# Database (SQLite is built into Python)
# No additional database dependencies needed

# Optional: For better logging
# (All logging modules are built into Python)

# Development/Testing (optional)
# pytest==8.3.3
# pytest-cov==5.0.0