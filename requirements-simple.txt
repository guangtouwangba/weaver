# Simplified Requirements for ArXiv Paper Fetcher
# Core dependencies only

# YAML configuration
PyYAML==6.0.2

# HTTP requests for API calls and PDF downloads
requests==2.32.3

# Alibaba Cloud OSS SDK (optional, only needed when using OSS backend)
oss2==2.18.4

# Database (SQLite is built into Python)
# No additional database dependencies needed

# XML parsing (built into Python)
# No additional dependencies needed

# Optional: For better logging
# (All logging modules are built into Python)

# Development/Testing (optional)
# pytest==8.3.3
# pytest-cov==5.0.0