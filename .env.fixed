# ===================================
# RAG 最小化配置文件
# ===================================
# 
# 这是一个最小化的配置文件，只包含必要的基础配置
# 避免了复杂嵌套配置导致的解析错误
#

# ===================================
# 应用基础配置
# ===================================
DEBUG=false
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000

# ===================================
# CORS 配置 (JSON 格式)
# ===================================
CORS_ORIGINS=["*"]
CORS_METHODS=["*"]
CORS_HEADERS=["*"]

# ===================================
# 数据库配置
# ===================================
DATABASE__HOST=localhost
DATABASE__PORT=5432
DATABASE__NAME=ragdb
DATABASE__DRIVER=asyncpg
DATABASE__ECHO=false

# ===================================
# Redis 配置
# ===================================
REDIS__HOST=localhost
REDIS__PORT=6379
REDIS__DB=0

# ===================================
# 存储配置
# ===================================
STORAGE__PROVIDER=local
STORAGE__LOCAL_PATH=./storage

# ===================================
# 日志配置
# ===================================
LOGGING__LEVEL=INFO

# ===================================
# 注意事项
# ===================================
# 1. 不包含密码等敏感信息，使用默认值
# 2. 复杂的嵌套配置（如 Celery）使用代码中的默认值
# 3. 如需要修改高级配置，请在代码中直接修改默认值
